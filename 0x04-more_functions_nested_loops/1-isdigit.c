#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks if c is a digit
 *
 * @c: character to be checked
 *
 * Discription: checks if c is a digit
 *
 * Return: 1 if c is a digit or 0 otherwise
 */
int _isdigit(int c)
{
return (c >= 0 && c <= 9)
}
